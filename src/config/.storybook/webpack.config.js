// needed to fix core-js conflict occurring between storybook and firebase
// https://github.com/storybooks/storybook/issues/6204

// module.exports = ({ config, mode }) => {
//     console.log(config.resolve.alias);
//     delete config.resolve.alias["core-js"];
//     return config;
// };
